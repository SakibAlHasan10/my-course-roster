
const Card = ({card}) => {
    const {img, course_name,course_description, course_price, 
        course_credit } = card;
    return (
        <div>
            
        </div>
    );
};

export default Card;